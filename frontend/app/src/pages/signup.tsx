import React from "react";

const SignupPage = () => {
    return (
        <main style={{ padding: 32 }}>
            <h1>サインアップページ</h1>
            {/* ユーザー名・メールアドレス・パスワード入力欄、登録ボタン、ログインページへのリンクをここに追加 */}
        </main>
    );
};

export default SignupPage;
