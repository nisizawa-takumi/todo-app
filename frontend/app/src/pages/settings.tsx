import React from "react";

const SettingsPage = () => {
    return (
        <main style={{ padding: 32 }}>
            <h1>ユーザー設定ページ</h1>
            {/* ユーザー情報表示・編集、パスワード変更などをここに追加 */}
        </main>
    );
};

export default SettingsPage;
